<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Piwigo API + image downloads -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Set the device wallpaper -->
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <!-- The slideshow runs in a foreground service so it keeps its schedule
         when the app is closed: Doze defers deferrable work well past the
         delay the user picked, and several manufacturers cancel scheduled
         work when the app is swiped away. No predefined service type covers
         "change the wallpaper at a user-chosen interval", hence specialUse. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- The service's own notification, which also controls the slideshow. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Images added as a local theme go through the system document picker,
         which grants access to the chosen files only. No storage permission
         is needed, which also avoids the Play Store photo-and-video
         declaration. Removed explicitly in case a plugin contributes them. -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove"/>

    <!-- MainApplication owns the cached FlutterEngine (see its Kotlin doc:
         Android 12+ recreates activities on every wallpaper change). -->
    <application
        android:label="@string/app_name"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Slideshow service. Started only while the user has a slideshow
             enabled, stopped as soon as every target is turned off. -->
        <service
            android:name=".RotationForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled wallpaper slideshow that must change the wallpaper and lock screen at the exact interval chosen by the user, including while the app is closed. The persistent notification shows the active themes and offers pause and next-image controls."/>
        </service>

        <!-- Wakes the device to apply the next wallpaper (see RotationAlarms). -->
        <receiver
            android:name=".RotationAlarmReceiver"
            android:exported="false"/>

        <!-- WorkManager declares a typeless foreground service for expedited
             work this app never requests; drop it so the only foreground
             service shipped is the one above. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            tools:node="remove"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

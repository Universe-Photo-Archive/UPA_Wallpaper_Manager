<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Piwigo API + image downloads -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Set the device wallpaper -->
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <!-- Background rotation runs as a plain WorkManager job, never as a
         foreground service. WorkManager's own manifest still contributes
         FOREGROUND_SERVICE (it needs it only for expedited / long-running
         work, which this app never requests); leaving it in would make the
         Play Store ask for a foreground-service justification. Removing it is
         safe as long as no worker calls setForeground/setForegroundAsync. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:node="remove"/>

    <!-- Images added as a local theme go through the system document picker,
         which grants access to the chosen files only. No storage permission
         is needed, which also avoids the Play Store photo-and-video
         declaration. Removed explicitly in case a plugin contributes them. -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove"/>

    <!-- MainApplication owns the cached FlutterEngine (see its Kotlin doc:
         Android 12+ recreates activities on every wallpaper change). -->
    <application
        android:label="UPA Wallpaper Manager"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Counterpart of the FOREGROUND_SERVICE removal above: drop the
             foreground service WorkManager declares but this app never starts. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            tools:node="remove"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

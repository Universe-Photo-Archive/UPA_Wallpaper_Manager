import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — actual widget tests require provider overrides
    expect(1 + 1, equals(2));
  });
}
